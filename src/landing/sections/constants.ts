/** Shared style/data constants for the landing page sections (extracted from Landing.tsx). */
export const REVEAL_DIRS = ['left', 'up', 'right'] as const;

export const kicker = 'text-[13px] font-extrabold tracking-[.14em] uppercase text-[var(--soft)]';
export const sectionTitle =
  "font-['Bricolage_Grotesque',sans-serif] text-[clamp(30px,4vw,44px)] font-extrabold text-[var(--ink)] m-[10px_0_0] tracking-[-0.01em]";
